<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>회원가입 | 여행어때</title>
  <link rel="shortcut icon" href="../static/img/favicon.ico">
  <link rel="stylesheet" type="text/css" href="../static/css/style.css">
  <link rel="stylesheet" type="text/css" href="../static/css/common.css">
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script>
    function signUp() {
      let form = document.querySelector(".signup-form");
      const formData = new FormData();
      const file = document.getElementById("profileImg");
      formData.append('id', form.id.value);
      formData.append('pw', form.pw.value);
      formData.append('name', form.name.value);
      formData.append('userfile', file.files[0]);
      axios({
        method: 'post',
        url: '/user/signup',
        data: formData,
        headers: {
          'Content-Type': 'multipart/form-data' // enctype
        }
      }).then((res) => {
        if (res.data) {
          form.id.value = "";
          form.pw.value = "";
          form.name.value = "";
          alert('회원가입 성공!');
          document.location.href = "/user/login";
        } else {
          alert("회원가입 실패! 중복된 아이디 또는 닉네임입니다!");
          form.reset();
        }
      })
    }

  </script>
</head>

<body>
  <form class="signup-form">
    <a href="http://localhost:8000/" class="login-logo" title="여행어때">여행어때</a>
    <br>
    <span class="signup-p">안녕하세요. <br>여행어때에 오신 것을 환영합니다.</span>
    <h6>국내여행 NO.1 플랫폼 여행어때</h6>
    <br>
    <input class="login-input" type="text" name="id" placeholder="아이디">
    <br>
    <br>
    <input class="login-input" type="text" name="pw" placeholder="비밀번호">
    <br>
    <br>
    <input class="login-input" type="text" name="name" placeholder="닉네임">
    <br>
    <br>
    <span>프로필 사진 :</span>
    <input type="file" id="profileImg" name="profileImg" placeholder="프로필">
    <br>
    <br>
    <button type="button" class="login-btn" onclick="signUp();">회원가입</button>
  </form>
</body>

</html>